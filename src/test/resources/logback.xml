<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console Appender: Logs to terminal -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender: Logs to a rotating file in target/logs -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>target/logs/test.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>target/logs/test-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Optional Allure Appender -->
    <!-- Uncomment if allure-logback dependency is present in pom.xml -->
    <!--
    <appender name="ALLURE" class="io.qameta.allure.logback.AllureLogbackAppender"/>
    -->

    <!-- Root Logger: Apply to all logs -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <!-- Uncomment below line if ALLURE appender is included -->
        <!-- <appender-ref ref="ALLURE"/> -->
    </root>

    <!-- Application Specific Logger -->
    <logger name="com.tests" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <!-- Uncomment below line if ALLURE appender is included -->
        <!-- <appender-ref ref="ALLURE"/> -->
    </logger>

</configuration>
